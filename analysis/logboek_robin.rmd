---
title: "Logboek"
author: "Robin Offringa"
output: html_document
---

# Slaap en hartslag — logboek

## Introductie

Slaap is een van de belangrijkste processen voor herstel van lichaam en hersenen. Tijdens de slaap vertraagt de hartslag, wat vaak aangeeft dat het lichaam tot rust komt.
Een lagere hartslag tijdens slaap wordt daarom vaak gezien als een aanwijzing voor betere slaapkwaliteit.
Verschillende factoren zoals stress, fysieke activiteit en avondgedrag beïnvloeden de hartslag overdag en tijdens slaap.

In dit project onderzoeken we of betere slaapkwaliteit samenhangt met een lagere gemiddelde hartslag.
Drie proefpersonen dragen een Fitbit-smartwatch en meten continu hartslag en slaap gedurende vier weken.
Dagelijks vullen zij een korte vragenlijst in over beweging, alcoholgebruik, eetgedrag en schermgebruik voor het slapen.


## Data en methode

- Data: Fitbit-export met afgeleide output in `analysis/data/combined_data_robin.csv`
- Tijdspanne: vier weken per proefpersoon
- Analyse: gemiddelde hartslag tijdens slaap, gemiddelde daghartslag, stappen per dag, en eenvoudige regressies met slaapscore als uitkomst

## 2026-05-20 — Opsplitsing naar Rmd en correlatie stappen en hartslag analyse

Vandaag heb ik mijn eerdere R-script opgesplitst naar een R Markdown-bestand zodat ik losse codechunks kan runnen en direct resultaten (tabellen/plots) kan tonen.
Hieronder staan korte voorbeelden van het inlezen en verkennen van de Fitbit-data uit de export.
Ik heb het script opgesplitst naar een Rmd zodat ik stukjes code interactiv run en opmerkingen bij de resultaten kan zetten.

### Geknipte analysevoorbeelden (kort)

Hieronder wat snippets met korte toelichting.

```{r load-data, echo=TRUE}
# Libraries + inlezen (pas paden aan indien nodig)
library(ggplot2); library(dplyr)
sleep_data <- read.csv("/Users/robinoffringa/Desktop/De-Wetenschappelijke-Cyclus/data/takeout_robin/Fitbit/Sleep Score/sleep_score.csv")
hr_data    <- read.csv("/Users/robinoffringa/Desktop/De-Wetenschappelijke-Cyclus/analysis/data/combined_data_robin.csv")
```

lees de sleepscore en hartslagbestanden in.

```{r prep-data, echo=TRUE}
# Snel samenvoegen op datum
sleep_data$date <- as.Date(sleep_data$timestamp)
hr_data$date    <- as.Date(hr_data$date)
data <- merge(
	sleep_data[, c("date","overall_score","resting_heart_rate")],
	hr_data[, c("date","average_heart_rate","total_steps")],
	by = "date"
)
data <- data[complete.cases(data), ]
```

samenvoegen en NA-waarden verwijderen zodat analyses op hele dagen lopen.

```{r key-analysis, echo=TRUE, fig.width=10, fig.height=4}
# Belangrijkste statistische test en visualisatie (rusthartslag)
corr2 <- cor.test(data$overall_score, data$resting_heart_rate, alternative = "two.sided")
print(corr2)

ggplot(data, aes(x = resting_heart_rate, y = overall_score)) +
	geom_point(alpha = 0.7) +
	geom_smooth(method = "lm", color = "red") +
	labs(title = "Slaapscore vs Rusthartslag",
			 x = "Rusthartslag (bpm)", y = "Slaapscore") +
	theme_minimal()
```

Dit test of een lagere rusthartslag correleert met hogere slaapscore.

```{r steps-analysis, echo=TRUE, fig.width=10, fig.height=4}
# Extra vergelijking: stappen per dag vs slaapscore
corr3 <- cor.test(data$overall_score, data$total_steps, alternative = "two.sided")
print(corr3)

ggplot(data, aes(x = total_steps, y = overall_score)) +
	geom_point(alpha = 0.7, color = "orange") +
	geom_smooth(method = "lm", color = "red") +
	labs(title = "Slaapscore vs Stappen per dag",
			 x = "Stappen per dag", y = "Slaapscore") +
	theme_minimal()
```

Hiermee kan ik zien of meer stappen per dag samenhangen met een hogere slaapscore.

## Observatie — Correlaties vergeleken

Bij vergelijking van de correlaties tussen slaapscore en de twee variabelen valt een interessant patroon op:

- **Slaapscore vs Hartslag**: Er is een duidelijke negatieve correlatie. betere slaapscores gaan samen met een lagere hartslag (zowel rusthartslag als gemiddelde daghartslag).
- **Slaapscore vs Stappen per dag**: Hier is bijna geen correlatie te zien. het aantal stappen per dag heeft vrijwel geen verband met de slaapscore.

Dit geeft aan dat hartslag een betere indicator is voor slaapkwaliteit dan fysieke activiteit (gemeten in stappen). 

## To do's

- Data van groepsgenoten toevoegen en vergelijken
- Eigen data vernieuwen met nieuwe export
- Google Form data exporteren en integreren voor aanvullende analyses (schermgebruik, etc.)
- R^2 analyse toevoegen om procenten verklaarde variantie te berekenen
- lm() functie gebruiken om meerdere variabalen tegelijk te analyseren (hartslag + stappen etc) en p waarden vergelijken

## 2026-05-21 — R^2 analyse en lm() functie toegevoegd

Vandaag heb ik de R^2-waarden toegevoegd aan de correlatietests zodat we kunnen zien hoeveel van de variantie in slaapscore wordt verklaard door hartslag en stappen.
Daarnaast heb ik een lineair model gemaakt met zowel hartslag als stappen als voorspellers van slaapscore, zodat we kunnen zien welke variabele sterker bijdraagt als we ze samen in één model
zetten.

Ik kon gister niet goed slapen en had veel stress. Mijn slaapscore was 70, lager dan mijn gemiddelde.
Ik vraag me af of mijn hartslag hoger was die dag.

Ik kwam er achter dat de fitbit elke dag een hoeveelheid verbrandde calorieën rapporteert,
wat waarschijnlijk een betere maat is voor fysieke activiteit dan stappen. 
Ik kwam er ook achter dat de fitbit elke dag een stressmanagement score geeft, gebaseerd op hartslagvariabiliteit.
Ik ga van deze data ook kijken of die samenhangt met slaapkwaliteit.


## To do's

- Calorieën en stressmanagement score toevoegen aan de analyses
- Eigen data vernieuwen met nieuwe export
- Google Form data exporteren en integreren voor aanvullende analyses (schermgebruik, etc.)


Verder hebben Lucas en Mohammed hun Fitbit data gedeeld, dus kan ik deze data vergelijken met mijn eigen data.

## 2026-05-22 — Ziek

Vandaag heb ik weer slecht geslapen en voel ik me ziek. Ik ga vandaag niet zo veel doen.
In de les analyseren we de githubs van klasgenoten en bespreken we de data die ze hebben verzameld.

## To do's

- Calorieën en stressmanagement score toevoegen aan de analyses
- Eigen data vernieuwen met nieuwe export
- Google Form data exporteren en integreren voor aanvullende analyses (schermgebruik, etc.)
- Python code opsplitsen in meer functies voor leesbaarheid en wijzigbaarheid
- Data van Mohammed toevoegen aan analyses en vergelijken met mijn data


## 2026-06-01 — Bespreking in de klas

Gisteren hebben we in de klas nog eens besproken wie wat gaat doen.
Ik ga verder met het Python-bestand en met mijn slaap en hartslagdata en extra analyses maken met de nieuwe data van Lucas en Mohammed.
We moesten ook even uitvogelen van wie welke data was, want er was verwarring over van wie welke data was omdat onze namen er niet bij stonden.
Gelukkig had ik mijn data op mijn telefoon apart opgeschreven en zo konden we achterhalen van wie welke data was.
Ik ga ook verder om elke dag de Google Form in te vullen en de Fitbit te dragen.


## 2026-06-02 — Python-bestand afgemaakt

Vandaag heb ik het Python-bestand afgemaakt en opgesplitst in functies. Ik heb data als calorieen en het Google formulier ook toegevoegd.
Verder heb ik alle gecombineerde data bestanden in een mapje gezet en een apart mapje voor alleen de dagen met een slaapscore.
Ik kon gister niet in slaap vallen en heb daardoor niet lang geslapen. Ik ben benieuwd of dat te zien is in de data.


Een klein stuk uit de code is bijvoorbeeld:

```python
print(f"Saving results to {output_file}...")
with open(output_file, 'w', newline='') as f:
	fieldnames = ['date', 'average_heart_rate', 'min_heart_rate', 'max_heart_rate', 'start', 'end', 'sleep_score', 'calories_burned', 'total_steps', 'phone_last_used', 'last_food_time', 'last_food']
	writer = csv.DictWriter(f, fieldnames=fieldnames)
	writer.writeheader()
	writer.writerows(results)

print(f"Saving sleep score results to {sleep_score_output_file}...")
with open(sleep_score_output_file, 'w', newline='') as f:
	fieldnames = ['date', 'average_heart_rate', 'min_heart_rate', 'max_heart_rate', 'start', 'end', 'sleep_score', 'calories_burned', 'total_steps', 'phone_last_used', 'last_food_time', 'last_food']
	writer = csv.DictWriter(f, fieldnames=fieldnames)
	writer.writeheader()
	writer.writerows(sleep_score_results)
```

## To do's

- Stressmanagement score toevoegen aan de analyses
- Eigen data vernieuwen met nieuwe export
- Data van Mohammed en Lucas toevoegen aan analyses en vergelijken met mijn data
- Extra analyses maken met de nieuwe data

## 2026-06-03 — REM, diepe slaap en stressmanagement score toegevoegd

Vandaag heb ik de REM-slaap, diepe slaap en stressmanagement score toegevoegd aan de data. Ik ben benieuwd of deze variabelen ook invloed hebben op de slaapscore.

## To do's

- Eigen data vernieuwen met nieuwe export
- Data van Mohammed en Lucas toevoegen aan analyses en vergelijken met mijn data
- Extra analyses maken met de nieuwe data
- Discussie en conclusie schrijven