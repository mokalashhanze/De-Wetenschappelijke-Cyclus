TemperatureEvents.json

This file contains events logged by your Fitbit device's temperature system.

Each entry is a JSON object that includes one of the following fields:
   - temperature_reading
   - temperature_histogram
   - temperature_monitor_mode
